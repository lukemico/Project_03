
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>


<h1><%= @post.title %></h1>

<p><%= @post.content %></p>

<% @images.each do |img| %>
  <%= image_tag img["display_src"] %>

<% end %>
