<!DOCTYPE html>
<html>
  <head>
    <title>Vision</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script src="js/medium-editor.js"></script>
    <script>
      var editor = new MediumEditor('.editable');
    </script>

  </head>

  <body>

    <%= flash[:error] %>

    <nav class="main-nav">
      <div class="main-nav-home"> <%= link_to "Home", home_index_path(@current_user)  %> </div>

      <% if @current_user %>
      <%= link_to(user_path(@current_user.id)) do %>Hello,
      <%=@current_user.first_name%>

      <div class="main-nav-edit"><%= link_to "Edit Profile", edit_user_path(@current_user) %> </div>
      <div class="main-nav-logout"><%= link_to "Log Out", "/logout", method: :delete %> </div>
      <% end %>
    <% else %>
      <%= link_to "Login", "/login" %>
      <%= link_to "Sign Up", "/users/new" %>
      <% end %>
    </nav>

<hr>

<h1>V-I-S-I-ö-N</h1>

      <div class="col-lg-6">
        <div class="input-group">
          <input type="text" class="form-control" placeholder="Search for...">
          <span class="input-group-btn">
            <button class="btn btn-default" type="button">Go!</button>
          </span>
        </div><!-- /input-group -->
      </div><!-- /.col-lg-6 -->
    </div><!-- /.row -->

    <%= yield %>

    <script>
      <% if @current_user %>
      var userID =
      <%= @current_user.id %>;
      <% end %>
    </script>
  </body>
</html>
